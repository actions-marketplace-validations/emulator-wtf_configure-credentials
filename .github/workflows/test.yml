name: Test
permissions:
  contents: read
  id-token: write
on:
  pull_request:
  push:
    branches:
    - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      with:
        persist-credentials: false
    - uses: jdx/mise-action@v4.2.4 # zizmor: ignore[unpinned-uses] immutable tag
    - run: mise lint
    - uses: ./
      with:
        oidc-configuration-id: '414eac8b-5d17-4860-915f-37329ceb1dff'
    - uses: emulator-wtf/actions/setup-ew-cli@v1.1.0 # zizmor: ignore[unpinned-uses] immutable tag
    - run: ew-cli --models
